{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug ray (cone.json)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/ray",
      "args": ["scenes/cone.json", "-o", "cone.png"],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "lldb",
      "preLaunchTask": "build"
    }
  ]
}
